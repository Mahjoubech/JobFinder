import { CompanyInitialsPipe } from './company-initials-pipe';

describe('CompanyInitialsPipe', () => {
  it('create an instance', () => {
    const pipe = new CompanyInitialsPipe();
    expect(pipe).toBeTruthy();
  });
});
