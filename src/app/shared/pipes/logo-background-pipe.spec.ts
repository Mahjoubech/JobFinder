import { LogoBackgroundPipe } from './logo-background-pipe';

describe('LogoBackgroundPipe', () => {
  it('create an instance', () => {
    const pipe = new LogoBackgroundPipe();
    expect(pipe).toBeTruthy();
  });
});
